<legend>New Story</legend>
<div class="col-sm-5">
<%= form_for(@story) do |f| %>

    
            <%= f.label :title %>
            <%= f.text_field :title %>
      
    
     
        <%= f.label :url %>
        <%= f.text_field :url %>
        
        <%= f.submit "Submit", class: "btn btn-primary" %>

<% end %>
</div>
